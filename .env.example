# Database — get a free one at https://neon.tech (copy the connection string)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/villa_management?schema=public"

# NextAuth — generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-secret-key-here-change-this"

# App URL
NEXTAUTH_URL="http://localhost:3000"
